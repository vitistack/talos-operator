apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "talos-operator.fullname" . }}
  labels:
    {{- include "talos-operator.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "talos-operator.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "talos-operator.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "talos-operator.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            # Endpoint mode configuration
            - name: ENDPOINT_MODE
              value: {{ .Values.config.endpointMode | quote }}
            {{- if .Values.config.customEndpoint }}
            - name: CUSTOM_ENDPOINT
              value: {{ .Values.config.customEndpoint | quote }}
            {{- end }}
            # Log configuration
            - name: LOG_LEVEL
              value: {{ .Values.config.logLevel | quote }}
            - name: LOG_JSON
              value: {{ .Values.config.logJson | quote }}
            # Secret and naming configuration
            {{- if .Values.config.secretPrefix }}
            - name: SECRET_PREFIX
              value: {{ .Values.config.secretPrefix | quote }}
            {{- end }}
            - name: VITISTACK_NAME
              value: {{ .Values.config.vitistackName | quote }}
            - name: NAME_KUBERNETES_PROVIDER
              value: {{ .Values.config.kubernetesProviderName | quote }}
            # Tenant configuration
            - name: TENANT_CONFIGMAP_NAME
              value: {{ .Values.config.tenant.configMapName | quote }}
            - name: TENANT_CONFIGMAP_NAMESPACE
              value: {{ .Values.config.tenant.configMapNamespace | quote }}
            - name: TENANT_CONFIGMAP_DATA_KEY
              value: {{ .Values.config.tenant.configMapDataKey | quote }}
            # Talos configuration
            - name: TALOS_VERSION
              value: {{ .Values.config.talos.version | quote }}
            - name: DEFAULT_KUBERNETES_VERSION
              value: {{ .Values.config.talos.defaultKubernetesVersion | quote }}
            - name: TALOS_PREDICTABLE_NETWORK_NAMES
              value: {{ .Values.config.talos.predictableNetworkNames | quote }}
            - name: TALOS_VM_INSTALL_IMAGE_KUBEVIRT
              value: {{ .Values.config.talos.vmInstallImage.kubevirt | quote }}
            - name: TALOS_VM_INSTALL_IMAGE_DEFAULT
              value: {{ .Values.config.talos.vmInstallImage.default | quote }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
